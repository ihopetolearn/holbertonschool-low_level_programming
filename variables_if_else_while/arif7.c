/**
 * this is the simple
 * edition to communicate
 */
#include <stdio.h>
int main(void)
{
	char letter;

	for (letter = z; letter >= a; letter--)
	putchar(letter);
	putchar('\n');
	return(0);
}





