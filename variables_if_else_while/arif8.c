/**
 * this is the only line 
 * this is the not on,y line 
 */
#include <stdio.h>
 int main(void)
{
	int num;
	int char;

	for (num = '0'; num <= '16'; num++)
	if (num == '0'; num  <= '10'; num++)
	putchar((num % 10) + '0')
	else (letter = 'a'; nume >= 'f'; letter++)
	putchar(letter);
	putchar('\n')
	return (0);
}




